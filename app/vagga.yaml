containers:
  py:
    setup:
    - !Alpine v3.4
    - !Install [ca-certificates, strace]
    - !Py3Install [six, mimeparse, falcon, werkzeug]

commands:
  run: !Command
    container: py
    run: [python3, -m, werkzeug.serving, shakespeare:app]
