containers:
  py:
    setup:
    - !Alpine v3.4
    - !Install [ca-certificates, strace]
    - !PipConfig { dependencies: true }
    - !Py3Install [falcon, gunicorn]

commands:
  run: !Command
    container: py
    run: [gunicorn, shakespeare:app]
